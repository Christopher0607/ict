"""Pre-registered strategy search.

The grid, the engine, and the decision rule are all committed before a search
runs. See research/search/registry.py -- a grid chosen after seeing results is
a selection, not a grid, and every significance figure computed from it is
meaningless.
"""
