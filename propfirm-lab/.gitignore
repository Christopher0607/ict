__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/
# anchored: an unanchored 'data/' also swallowed research/data/ source files
/data/*
*.dbn.zst
*.parquet
.env

# ...but the exported dataset is the ONLY copy of paid data and must be kept.
# The Databento account can no longer afford to re-download it.
!/data/
!/data/*.parquet
!/data/*.csv
!/data/README.md

# Databento cache -- downloaded data costs money; never commit it
research/cache/
*.dbn
*.dbn.zst
